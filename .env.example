# Cloudinary (create an unsigned upload preset in Cloudinary)
VITE_CLOUDINARY_CLOUD_NAME=dcjmaapvi
VITE_CLOUDINARY_UPLOAD_PRESET=tracker

# Optional: full API URL (don't store secrets in client-side env)
# CLOUDINARY_URL=cloudinary://<api_key>:<api_secret>@dcjmaapvi
